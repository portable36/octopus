/** @deprecated Import from `@/components/ui/field` — re-export for vendor catalog callers. */
export {
  fieldClass,
  labelClass,
  formClass,
  hintClass,
  checkboxClass,
  Field,
} from '@/components/ui/field';

export const sectionNavClass =
  'rounded-md px-3 py-2 text-left text-sm transition-colors hover:bg-muted';
export const sectionNavActiveClass = 'bg-muted font-medium';
